import { DEBUG_CONFIG } from '../debug/debug.config';

export const environment = {
  production: false,
  supabaseUrl: '',
  supabaseAnonKey: '',
  useMockData: true,
  debugConfig: DEBUG_CONFIG,
};
